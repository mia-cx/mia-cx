{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "./tsconfig.node20.json",
    "display": "mia-cx tsup tsconfig",
    "compilerOptions": {
        "module": "preserve",
        "target": "esnext",
        "moduleResolution": "bundler",
        "ignoreDeprecations": "6.0"
    },
    "_comment": "ignoreDeprecations: tsup still passes baseUrl to its declaration build; drop this once tsup stops."
}
