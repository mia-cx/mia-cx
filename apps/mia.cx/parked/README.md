Routes taken off the site but kept for when Svartz powers the content. Move a folder back under
`src/routes/` to restore it; nothing outside `src` is built or type-checked.

- `blog/[slug]` — individual post pages, rendered from `vault/02 - Blog`.
